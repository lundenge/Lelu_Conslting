import type { Metadata } from "next";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import "./globals.css";

export const metadata: Metadata = {
  title: "Lelu-Consulting | Software Engineering, AI & Data Analytics",
  description: "Innovative technology consulting specializing in Software Engineering, Artificial Intelligence, Machine Learning, and Data Analytics solutions.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
